{
  "name": "twitter-account-migration",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build && npm run copy-html",
    "copy-html": "mkdir -p dist/src/functions/templates && cp src/functions/templates/*.html dist/src/functions/templates",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@types/handlebars": "^4.1.0",
    "handlebars": "^4.7.8"
  },
  "devDependencies": {
    "@types/node": "^18.x",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  },
  "main": "dist/src/functions/*.js"
}
